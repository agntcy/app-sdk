[project]
name = "agntcy-mcp-examples"
version = "0.1.0"
description = "Practical MCP monitoring examples using the Agntcy Application SDK"
requires-python = ">=3.12,<4.0"
license = "Apache-2.0"
dependencies = [
    "agntcy-app-sdk",
    "psutil>=6.0.0",
    "docker>=7.0.0",
]
